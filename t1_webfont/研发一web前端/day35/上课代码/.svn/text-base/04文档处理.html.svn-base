<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title></title>
		<script src="../../day34/上课代码/js/jquery-3.4.1.js" type="text/javascript" charset="utf-8"></script>
		<script type="text/javascript">
			$(function(){
				//创建标签
				var p1 = $("<p></p>"); 
				p1.html("呵呵");
				//向每个匹配的元素内部追加内容。
				//$(".box").append(p1);
				
				//把所有匹配的元素追加到另一个指定的元素元素集合中。
				//p1.appendTo($(".box"));
				
				//向每个匹配的元素内部前置内容。
				//$(".box").prepend(p1);
				
				//在每个匹配的元素之后插入内容。
				//$('.box').after(p1);
				
				//在每个匹配的元素之前插入内容。
				//$('.box').before(p1);
				
				//将所有匹配的元素替换成指定的HTML或DOM元素。
				//$(".box").replaceWith(p1);
				
				//从DOM中删除所有匹配的元素。
				//$(".box").remove();
				
				//克隆匹配的DOM元素并且选中这些克隆的副本。
				//$(".box").after($(".box").clone())
				
				$(".btn").click(function(){
					console.log("哈哈");
				})
				$(".box").after($(".btn").clone(true));
			})
		</script>
	</head>
	<body>
		<div class="box" id="obox" title="hehe">
			<h3>hello word</h3>
		</div>
		<button class="btn">按钮</button>
	</body>
</html>
